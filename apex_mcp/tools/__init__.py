"""Tool modules for Oracle APEX MCP Server.

Each sub-module groups related MCP tools by domain:

- ``sql_tools``: Connection, SQL execution, status
- ``app_tools``: Application lifecycle (create, finalize, delete, export)
- ``page_tools``: Page management
- ``component_tools``: Regions, items, buttons, processes, dynamic actions
- ``shared_tools``: LOVs, auth schemes, nav items, app items/processes
- ``schema_tools``: Table/view introspection and FK detection
- ``generator_tools``: High-level generators (CRUD, dashboard, login)
- ``visual_tools``: JET charts, gauges, funnels, sparklines, metric cards
- ``chart_tools``: Advanced chart types (stacked, combo, pareto, scatter, etc.)
- ``ui_tools``: 20 rich HTML UI components (hero banner, KPI row, etc.)
- ``advanced_tools``: Wizard, interactive grid, faceted search, and more
- ``inspect_tools``: Query and modify existing APEX apps
- ``js_tools``: Page/global JavaScript, AJAX handlers
- ``devops_tools``: REST endpoints, page export, docs, batch mode
- ``validation_tools``: Item validations and computations
- ``setup_tools``: Setup guide, requirements check, permissions
- ``user_tools``: APEX workspace user management
"""
